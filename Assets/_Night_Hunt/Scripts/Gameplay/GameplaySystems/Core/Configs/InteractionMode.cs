namespace NightHunt.GameplaySystems.Core.Configs
{
    /// <summary>
    /// Shared interaction mode enum used by both LootableConfig and InteractableConfig.
    /// </summary>
    public enum LootInteractionMode
    {
        Instant,    // Bấm 1 phát là tương tác
        Hold        // Giữ nút để tương tác
    }
}
